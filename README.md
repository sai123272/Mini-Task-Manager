# Mini Task Manager

Simple task management app — signup/login, create/edit/delete tasks, mark complete.

## Tech stack
- Frontend: React + Vite (+ optional Tailwind)
- Backend: Node.js + Express, SQLite
- Auth: JWT
- Containerization: Docker & docker-compose

## Run (one-command)
1. Copy `.env.example` files to `.env` in *backend* and *frontend* and set values.
2. Build & start:
   ```bash
   docker-compose up --build
