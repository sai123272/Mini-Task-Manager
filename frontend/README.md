# React + Vite

This is the frontend for the **Mini Task Manager** application.  
Built using **React + Vite**, it communicates with the backend through REST APIs.

---

## 🧰 Tech Stack
- **React**
- **Vite**
- **Fetch API**
- **LocalStorage (JWT storage)**

To build:
    docker build -t task-manager-frontend .


To run:
    docker run -p 5173:5173 task-manager-frontend
